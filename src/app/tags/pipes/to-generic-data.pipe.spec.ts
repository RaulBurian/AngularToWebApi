import { ToGenericDataPipe } from './to-generic-data.pipe';

describe('ToGenericDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ToGenericDataPipe();
    expect(pipe).toBeTruthy();
  });
});
