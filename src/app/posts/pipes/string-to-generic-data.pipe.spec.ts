import { StringToGenericDataPipe } from './string-to-generic-data.pipe';

describe('StringToGenericDataPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToGenericDataPipe();
    expect(pipe).toBeTruthy();
  });
});
