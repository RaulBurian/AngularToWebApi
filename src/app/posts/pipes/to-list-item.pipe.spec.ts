import { ToListItemPipe } from './to-list-item.pipe';

describe('ToListItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ToListItemPipe();
    expect(pipe).toBeTruthy();
  });
});
